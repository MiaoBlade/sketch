

namespace Sketchpad
{
    public enum GridType
    {
        Square, None, Refresh, Hexgon
    }
    public enum PadState
    {
        Drag,
        Draw,
        Idle
    }
    public enum DrawMode
    {
        Pen,
        Erase
    }
}